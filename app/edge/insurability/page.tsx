// ------------------------------------------------------------
// Edge of Insurability — Canonical Entry
// Elevated for readability and visual hierarchy
// App Router | Next.js 16 SAFE
// ------------------------------------------------------------

import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "The Edge of Insurability",
  description:
    "A system is admissible only if risk is transferable through independently auditable control evidence.",
  openGraph: {
    title: "The Edge of Insurability",
    description:
      "Insurability defines the binary boundary where systems become economically accountable through provable risk control.",
    type: "article",
  },
};

export default function EdgeOfInsurabilityPage() {
  return (
    <main className="relative overflow-hidden bg-[#020817]">
      <div className="absolute inset-0 bg-[radial-gradient(circle_at_top,rgba(59,130,246,0.16),transparent_30%),radial-gradient(circle_at_85%_15%,rgba(14,165,233,0.10),transparent_22%),linear-gradient(to_bottom,rgba(255,255,255,0.02),transparent_32%)]" />
      <div className="absolute inset-0 opacity-[0.08] [background-image:linear-gradient(rgba(255,255,255,0.12)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.12)_1px,transparent_1px)] [background-size:48px_48px]" />

      <div className="relative mx-auto max-w-6xl px-6 py-16 md:px-8 lg:px-10 lg:py-20 space-y-8">
        {/* ===================================================== */}
        {/* HERO */}
        {/* ===================================================== */}
        <section className="rounded-[28px] border border-white/10 bg-gradient-to-br from-[#111827]/95 via-[#10151f]/92 to-black/92 p-8 shadow-[0_30px_120px_rgba(0,0,0,0.42)] backdrop-blur-xl md:p-10 lg:p-12">
          <div className="space-y-6">
            <h1 className="text-4xl font-semibold tracking-tight text-white md:text-5xl">
              The Edge of Insurability
            </h1>

            <div className="flex flex-wrap gap-3">
              <span className="rounded-full border border-white/12 bg-white/8 px-3 py-1 text-sm font-medium text-white/92">
                Edge Framework
              </span>
              <span className="rounded-full border border-white/12 bg-white/8 px-3 py-1 text-sm font-medium text-white/92">
                Economic Boundary
              </span>
              <span className="rounded-full border border-white/12 bg-white/8 px-3 py-1 text-sm font-medium text-white/92">
                Risk Transfer Constraint
              </span>
            </div>

            <p className="max-w-3xl text-lg leading-8 text-white/84">
              A system is admissible only if risk can be transferred through
              independently auditable control evidence. Absent proof, coverage
              does not attach, and economic accountability fails.
            </p>
          </div>
        </section>

        {/* ===================================================== */}
        {/* CORE DOCTRINE */}
        {/* ===================================================== */}
        <section className="rounded-[24px] border border-white/10 bg-black/80 p-8 shadow-[0_18px_60px_rgba(0,0,0,0.24)]">
          <p className="text-xl leading-8 text-white">
            Insurability is admissible only if risk is provably controllable.
          </p>
          <p className="mt-4 text-base leading-7 text-white/68">
            Any system that cannot demonstrate auditable control evidence
            constitutes structural economic failure.
          </p>
        </section>

        {/* ===================================================== */}
        {/* TESTED ASSUMPTION / WHY THIS MATTERS */}
        {/* ===================================================== */}
        <section className="grid gap-6 md:grid-cols-2">
          <div className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)]">
            <h2 className="text-xl font-semibold text-white">
              Tested Assumption
            </h2>
            <p className="mt-3 text-base leading-7 text-white/70">
              AI systems can demonstrate independently verifiable control over
              risk sufficient for underwriting and coverage.
            </p>
          </div>

          <div className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)]">
            <h2 className="text-xl font-semibold text-white">Why This Matters</h2>
            <p className="mt-3 text-base leading-7 text-white/70">
              Systems that cannot transfer risk remain economically unbounded,
              preventing accountability, scaling, and market integration.
            </p>
          </div>
        </section>

        {/* ===================================================== */}
        {/* SYSTEM DEFINITION */}
        {/* ===================================================== */}
        <section className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)] space-y-4">
          <h2 className="text-xl font-semibold text-white">System Definition</h2>
          <p className="text-base leading-7 text-white/70">
            Insurability is defined as the ability of a system to transfer
            financial risk through independently auditable evidence of
            governance, control, and operational discipline.
          </p>
        </section>

        {/* ===================================================== */}
        {/* GOVERNING VARIABLE */}
        {/* ===================================================== */}
        <section className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)] space-y-4">
          <h2 className="text-xl font-semibold text-white">Governing Variable</h2>
          <p className="text-base leading-7 text-white/70">
            Presence of independently verifiable control evidence sufficient to
            satisfy underwriting requirements for risk transfer.
          </p>
        </section>

        {/* ===================================================== */}
        {/* EXPERIMENTAL / BOUNDARY SETUP */}
        {/* ===================================================== */}
        <section className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)] space-y-4">
          <h2 className="text-xl font-semibold text-white">
            Experimental / Boundary Setup
          </h2>
          <ul className="list-disc pl-6 space-y-3 text-base leading-7 text-white/70 marker:text-white/45">
            <li>Evaluate existence of documented control frameworks</li>
            <li>Test auditability and third-party verifiability of controls</li>
            <li>Assess traceability of decisions, actions, and failures</li>
            <li>Verify enforcement of governance at runtime</li>
            <li>Determine insurer acceptance or rejection of risk transfer</li>
          </ul>
        </section>

        {/* ===================================================== */}
        {/* BINARY FALSIFICATION THRESHOLD */}
        {/* ===================================================== */}
        <section className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)] space-y-4">
          <h2 className="text-xl font-semibold text-white">
            Binary Falsification Threshold
          </h2>
          <p className="text-base leading-7 text-white/70">
            System fails if risk cannot be transferred due to absence of
            independently auditable control evidence.
          </p>
        </section>

        {/* ===================================================== */}
        {/* OPERATIONAL INTERPRETATION */}
        {/* ===================================================== */}
        <section className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)] space-y-5">
          <h2 className="text-xl font-semibold text-white">
            Operational Interpretation
          </h2>

          <ul className="list-disc pl-6 space-y-3 text-base leading-7 text-white/70 marker:text-white/45">
            <li>Capability is not admissible evidence</li>
            <li>Performance does not substitute for control</li>
            <li>Intent and internal assurance carry no weight</li>
            <li>All governance must be externally auditable</li>
            <li>Coverage is binary: attached or absent</li>
          </ul>

          <p className="text-base leading-7 text-white/70">
            Governance that cannot be proven is treated as nonexistent.
          </p>
        </section>

        {/* ===================================================== */}
        {/* BOUNDARY OF CLAIM */}
        {/* ===================================================== */}
        <section className="rounded-[22px] border border-white/12 bg-white/[0.035] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)] space-y-4">
          <h2 className="text-xl font-semibold text-white">Boundary of Claim</h2>
          <p className="text-base leading-7 text-white/70">
            This constraint evaluates economic accountability only. It does not
            assess technical performance, ethical intent, or regulatory posture.
          </p>
        </section>

        {/* ===================================================== */}
        {/* PASS / FAIL */}
        {/* ===================================================== */}
        <section className="grid gap-6 md:grid-cols-2">
          <div className="rounded-[22px] border border-emerald-400/24 bg-emerald-500/[0.08] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)]">
            <h3 className="text-lg font-semibold text-emerald-300">PASS</h3>
            <p className="mt-3 text-base leading-7 text-white/78">
              Risk is transferable through independently auditable control
              evidence accepted by insurers.
            </p>
          </div>

          <div className="rounded-[22px] border border-red-400/24 bg-red-500/[0.08] p-6 shadow-[0_18px_60px_rgba(0,0,0,0.2)]">
            <h3 className="text-lg font-semibold text-red-300">FAIL</h3>
            <p className="mt-3 text-base leading-7 text-white/78">
              Risk cannot be transferred due to missing, unverifiable, or
              insufficient control evidence.
            </p>
          </div>
        </section>

        {/* ===================================================== */}
        {/* INVARIANT */}
        {/* ===================================================== */}
        <section className="rounded-[24px] border border-white/10 bg-black/82 p-8 shadow-[0_18px_60px_rgba(0,0,0,0.24)]">
          <p className="text-xl leading-8 text-white">
            If risk cannot be transferred, accountability does not exist.
          </p>
          <p className="mt-4 text-base leading-7 text-white/68">
            Systems without insurability remain experimental. Economic reality
            begins only where risk is provably controlled and accepted.
          </p>
        </section>
      </div>
    </main>
  );
}
